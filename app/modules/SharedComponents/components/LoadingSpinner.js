
import React, { PropTypes } from 'react'

import {loader} from '../styles/LoadingSpinner.css'


const LoadingSpinner = () => (
        <div className={`${loader}`}></div>
)

export default LoadingSpinner
